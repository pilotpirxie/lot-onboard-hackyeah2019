{{> header}}
<div class="container-fluid mt-5">
    <div class="row">
        <div class="col-12 text-center">
            <h2>🎫 Your tickets</h2>
        </div>
    </div>
    <div class="row mt-3">
        <div class="col-12">
            <ul class="list-group">
                {{#each tickets}}
                    <li class="list-group-item">
                        <h5>🛫 {{this.from}} → {{this.to}}</h5>
                        <p>{{this.departure_time}} → {{this.arrival_time}}</p>

                            {{#if this.flight_status}}
                            <a href="/boarding-pass/{{this.user_id}}/{{this.flight_id}}">
                              <button type="button" class="form-control mt-2 btn btn-success ">Boarding pass</button>
                            {{else}}
                            <a href="/check-in">
                              <button type="button" class="form-control mt-2 btn btn-primary ">Check-in</button>
                             </a>
                            {{/if}}

                        <a href="/check-list/1">
                            <button type="button" class="form-control mt-2 btn btn-outline-primary ">Guide & Check-list</button>
                        </a>
                    </li>
                {{/each}}
            </ul>
        </div>
    </div>
</div>

{{> footer}}