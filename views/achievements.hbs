{{> header}}
<div class="container-fluid">
    <div class="row mt-5">
        <div class="col-12 text-center">
            <h2>🏆 ACHIEVEMENTS</h2>
        </div>
    </div>
    <div class="row mt-3">
        <div class="col-12 pb-4">
            <ul class="list-group">
                <li class="list-group-item font-weight-bold">Travel around the world and collect achievements</li>
            </ul>
            <div class="mt-2 mb-5">

              {{#each achievments}}
                <div class="card">
                    <div class="card-body">
                        <h5>{{this.name}}</h5>
                        <p>{{this.description}}</p>
                    </div>
                </div>
                 {{/each}}
            </div>
        </div>
    </div>
</div>

{{> footer}}